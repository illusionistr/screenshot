rules_version = '2';

// TEMPORARY: Open Firebase Storage rules for debugging
// TODO: Secure these rules before production
service firebase.storage {
  match /b/{bucket}/o {
    // COMPLETELY OPEN for debugging - REMOVE IN PRODUCTION
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}